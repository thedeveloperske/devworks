import { PageHeader } from "@/components/admin/PageHeader";

export default function DeclineReasonPage() {
  return (
    <PageHeader
      title="Decline Reason"
      description="Manage decline reasons used across the system"
    />
  );
}
