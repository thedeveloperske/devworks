import { PageHeader } from "@/components/admin/PageHeader";

export default function InvalidBillReasonPage() {
  return (
    <PageHeader
      title="Invalid Bill Reason"
      description="Manage invalid bill reasons used across the system"
    />
  );
}
