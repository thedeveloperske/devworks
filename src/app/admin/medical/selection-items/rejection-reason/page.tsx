import { PageHeader } from "@/components/admin/PageHeader";

export default function RejectionReasonPage() {
  return (
    <PageHeader
      title="Rejection Reason"
      description="Manage rejection reasons used across the system"
    />
  );
}
