import { PageHeader } from "@/components/admin/PageHeader";

export default function MemberStatusPage() {
  return (
    <div>
      <PageHeader
        title="Member Cancellation & Reinstate"
        description="This screen will be rebuilt. The feature is temporarily unavailable."
      />
      <p className="mt-4 border border-slate-200 bg-white px-4 py-3 text-[12px] text-slate-600">
        Cancellation and reinstate will return here once the new flow is ready.
      </p>
    </div>
  );
}
